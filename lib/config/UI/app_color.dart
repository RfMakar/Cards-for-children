import 'package:flutter/material.dart';

class AppColor {
  static const white = Colors.white;
  static const colorMain = Color.fromRGBO(135, 206, 235, 1);
   static const colorSecondary = Color.fromRGBO(235, 164, 135, 1);
  static final transparent = Colors.white.withOpacity(0);
}
